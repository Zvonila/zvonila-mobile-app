
export const useCallManager = () => {
//   const { setPC, setLocalOffer, setCurrentCall } = useCallStore();

  const createCall = async (user_id: number) => {
    return user_id
};

  return { createCall };
};
